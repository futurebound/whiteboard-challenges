'use strict';

const solution = require('../lib/solution');

describe('solution.js', function() {
  it('', function(done) {
    expect();
    done();
  });

  it('', function(done) {
    expect();
    done();
  });

  it('', function(done) {
    expect();
    done();
  });
});